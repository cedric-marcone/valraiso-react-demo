**Instructions pour jouer avec le projet**

  Il faut d'abord vous assurer d'avoir `node` et `npm`.
  (*Sur mac, `brew install node`*)

Pour installer le projet : `npm install`

Pour démarrer le projet : `npm start`
